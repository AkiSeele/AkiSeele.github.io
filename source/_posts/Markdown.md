---
title: Markdown语法
tags:
  - 笔记
categories:
  - 笔记
---
> **Markdown 是一种轻量级标记语言，它允许人们使用易读易写的纯文本格式编写文档，Markdown文件的后缀名便是“.md”。**
> Markdown的语法十分简单，常用的标记符号不超过十个
> [Markdown官方教程](https://markdown.com.cn/)
> 建议使用[typora](https://typoraio.cn/)编写.md文件,vsCode可安装[Markdown All in One](https://marketplace.visualstudio.com/items?itemName=yzhang.markdown-all-in-one)插件

## 标题语法
要创建标题，请在单词或短语前面添加井号 (#) 。
```
# 的数量代表了标题的级别。
例如，添加三个 # 表示创建一个三级标题 (<h3>) (例如：### My Header)。
```



## 段落、换行语法
```
<!-- 段落 -->
要创建段落，请使用空白行将一行或多行文本进行分隔。
不要用空格（spaces）或制表符（ tabs）缩进段落。

<!-- 换行 -->
在一行的末尾添加两个或多个空格，然后按回车键,即可创建一个换行(<br>)。
为了兼容性，请在行尾添加“结尾空格”或 HTML 的 <br> 标签来实现换行
```

## 强调语法
```
为兼容考虑请使用星号
<!-- 粗体 -->
前后各添加两个星号或下划线
例：**被加粗的文字**
快捷键 Ctrl + B

<!-- 斜体 -->
前后各添加一个星号或下划线
快捷键 Ctrl + I
例：*被斜体的文字* 
要同时用粗体和斜体突出显示文本，请在单词或短语的前后各添加三个星号或下划线。
```

## 引用语法
要创建块引用，请在段落前添加一个 > 符号。
块引用可以包含其他 Markdown 格式的元素。
```
> 引用语法展示
要嵌套的段落前添加一个 >> 符号。
> 外层
>> 中层
>>> 里层
```
> 外层
>> 中层
>>> 里层

## 列表语法
```
缩进一个或多个列表项可创建嵌套列表
要在保留列表连续性的同时在列表中添加另一种元素，请将该元素缩进四个空格或一个制表符
代码块通常采用四个空格或一个制表符缩进。当它们被放在列表中时，请将它们缩进八个空格或两个制表符
<!-- 有序列表 -->
在每个列表项前添加数字并紧跟一个英文句点。
例：
1. 第一条
2. 第二条
3. 第三条
    1. 里层第一条
    2. 里层第二条
4. 第四条
<!-- 无序列表 -->
在每个列表项前面添加破折号 (-)、星号 (*) 或加号 (+) 。
例：
- 第一条
- 第二条
- 第三条
    - 里层第一条
        - 最里层  
    - 里层第二条
- 第四条
```

## 代码语法
将单词或短语表示为代码，请将其包裹在反引号 (`) 中。
```
<!-- 转义反引号 -->
如果你要表示为代码的单词或短语中包含一个或多个反引号，则可以通过将单词或短语包裹在双反引号(``)中。

<!-- 代码块 -->
使用(```)创建代码块区域
```

## 分割线语法
单独一行上使用三个或多个星号 (***)、破折号 (---) 或下划线 (___) ，并且不能包含其他内容。

## 链接语法
链接文本放在中括号内，链接地址放在后面的括号中，链接title可选，可使用强调语法与（`）。

建议用法：复制好地址链接，选中需要变成链接的文字，直接粘贴
```
<!-- 链接title -->
放在圆括号中链接地址后面，跟链接地址之间以空格分隔
例：[超链接显示名](超链接地址 "超链接title")

使用尖括号可以很方便地把URL或者email地址变成可点击的链接。
<https://markdown.com.cn>
```

## 图片语法
使用感叹号 (!), 然后在方括号增加替代文本，图片链接放在圆括号里，括号里的链接后可以增加一个可选的图片标题文本
```
![上号](https://s1.ax1x.com/2022/10/10/xYfbJU.png)
```
![上号](https://s1.ax1x.com/2022/10/10/xYfbJU.png)

## 转义字符语法
显示原本用于格式化 Markdown 文档的字符，请在字符前面添加反斜杠字符 \ 。

## 内嵌HTML标签
对于 Markdown 涵盖范围之外的标签，都可以直接在文件里面用 HTML 本身。如需使用 HTML，不需要额外标注这是 HTML 或是 Markdown，只需 HTML 标签添加到 Markdown 文本中即可。
注：出于安全原因，并非所有 Markdown 应用程序都支持在 Markdown 文档中添加 HTML